# Onboarding Extension

Creates the first-run welcome conversation for new Neon Pilot installs when the user is still on a landing route.

The onboarding conversation explains the first useful steps: configure a provider in Settings, understand that Neon Pilot is extension-based, manage extensions and imported plugin packages from Settings → Extensions, then start a real task conversation.
