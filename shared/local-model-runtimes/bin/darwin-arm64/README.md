# Bundled llama.cpp runtime

Package Metal-enabled macOS arm64 llama.cpp runtime files in this directory:

- `llama-cli`
- `llama-server`
- the matching `*.dylib` files and compatibility symlinks from the same release archive

Do not commit downloaded model files here.
